# Apphive Docs

Documentación oficial de Apphive (https://docs.apphive.io) con MkDocs Material.
Bilingüe: español en `/`, inglés en `/en/`. Ver `mkdocs.yml` (es) y `mkdocs.en.yml` (en).
Deploy automático a GitHub Pages en cada push a `master`.
